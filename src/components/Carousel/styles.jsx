import styled from "styled-components";

export const CarouselLayout = styled.div`
    & .carousel-item{
        width: 750px;
        height: 400px;
        opacity: 0.6;
        filter: grayscale(1);
    }
`